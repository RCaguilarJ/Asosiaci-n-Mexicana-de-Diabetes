# .env.example - Copia a .env en tu entorno local y añade valores.
# NO subir .env al repositorio ni al servidor de producción.

# Entorno (development | production)
APP_ENV=development

# DB local (WAMP defaults)
DB_HOST=localhost
DB_NAME=diabetes_db
DB_USER=root
DB_PASS=

# Remote API (para pruebas locales apunta a la misma app si quieres)
REMOTE_API_URL=http://localhost/asosiacionMexicanaDeDiabetes
REMOTE_API_TOKEN=CHANGE_THIS_TO_A_STRONG_SECRET_TOKEN

# Remote DB fallback (opcional)
REMOTE_DB_HOST=
REMOTE_DB_NAME=
REMOTE_DB_USER=
REMOTE_DB_PASS=
