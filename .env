# Local .env (PRUEBAS LOCALES) — NO subir a producción
APP_ENV=development

# DB principal (Sistema Gestión Médica)
DB_HOST=localhost
DB_NAME=sistema_gestion_medica
DB_USER=root
DB_PASS=

# API Sistema de Gestión Médica (Node.js backend)
SISTEMA_GESTION_API_URL=http://localhost:4000/api
AMD_SYNC_SECRET=amd_diabetes_sync_key_2026_secure_hmac

# Base de datos remota (misma que principal para esta integración)
REMOTE_DB_HOST=localhost
REMOTE_DB_NAME=sistema_gestion_medica
REMOTE_DB_USER=root
REMOTE_DB_PASS=

# Legacy API settings (mantener compatibilidad)
AMD_API_BASE_URL=http://localhost:4000/api
REMOTE_API_URL=http://localhost/asosiacionMexicanaDeDiabetes
REMOTE_API_TOKEN=CHANGE_THIS_TO_A_STRONG_SECRET_TOKEN
API_SHARED_TOKEN=CHANGE_THIS_TO_A_STRONG_SECRET_TOKEN

# Remote DB fallback (opcional)
REMOTE_DB_HOST=
REMOTE_DB_NAME=
REMOTE_DB_USER=
REMOTE_DB_PASS=S
# .env.example - Copia a .env en tu entorno local y añade valores.
# NO subir .env al repositorio ni al servidor de producción.

# Entorno (development | production)
APP_ENV=development

# DB local (WAMP defaults)
DB_HOST=localhost
DB_NAME=sistema_gestion_medica
DB_USER=root
DB_PASS=

# Remote API (para pruebas locales apunta a la misma app si quieres)
REMOTE_API_URL=http://localhost/asosiacionMexicanaDeDiabetes
REMOTE_API_TOKEN=CHANGE_THIS_TO_A_STRONG_SECRET_TOKEN

# Remote DB fallback (opcional)
REMOTE_DB_HOST=
REMOTE_DB_NAME=
REMOTE_DB_USER=
REMOTE_DB_PASS=